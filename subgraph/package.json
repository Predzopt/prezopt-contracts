{
  "name": "prezopt-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ prezopt-protocol",
    "deploy-testnet": "graph deploy --network blockdag-testnet prezopt-protocol",
    "create-local": "graph create --node http://localhost:8020/ prezopt-protocol",
    "remove-local": "graph remove --node http://localhost:8020/ prezopt-protocol",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 prezopt-protocol"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.56.0",
    "@graphprotocol/graph-ts": "0.30.0"
  }
}